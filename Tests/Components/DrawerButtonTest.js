import 'react-native'
import React from 'react'
import DrawerButton from '../../App/Components/DrawerButton'
import { shallow } from 'enzyme'
import renderer from 'react-test-renderer'

test('DrawerButton component renders correctly', () => {
  // SnapshotTest
})

test('onPress', () => {
  // test onPress functionnality here
  // mock a function and write a test before and after
  // click to prove that the component works as expected
})
